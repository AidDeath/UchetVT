<Window x:Class="UchetVT.AccessAssign"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UchetVT"
        mc:Ignorable="d"
        Title="Права доступа" Height="482.41" Width="332.548" WindowStartupLocation="CenterOwner" Icon="Resources/rfid_chip.png" WindowStyle="ToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ListBox Name="AccessList" Grid.Row="0" Margin="5"></ListBox>
        <DockPanel  Grid.Row="1" Dock="Bottom" Margin="0,3">
            <Button IsDefault="True" Width="100" Margin="50,0,0,0">OK</Button>
            <Button IsCancel="True" Width="100">Отмена</Button>
        </DockPanel>
    </Grid>
</Window>
